# Texture Shadow

This example shows a shadow that is cast based on the shape of a texture, so
that only non-transparent parts of the texture are casting shadow.

<live-code src="./example.html"></live-code>
